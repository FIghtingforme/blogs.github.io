---

title:  "AWS-SAA"    # 建议加双引号，防止标题中有冒号或特殊字符导致解析失败"
date: 2026-05-12T8:40:00+08:00 # 建议带上 ISO 8601 时间戳，方便精准排序和时区对齐
categories: ["Certificates"]         # 使用标准的 YAML 数组格式 []，可扩展性更好
tags: ["AWS-SAA"]    # 强烈建议增加 tags 字段，这在 Hugo 中是默认的二级索引
draft: false                  # 显式声明发布状态，防止漏发

---



##　AWS SAA-C03 认证：


### Step1 (20%):  
- 看B站Stephane Maarek


### Step2:  (30%): 
- 注册AWS-free tier

- 建一个 VPC，划分子网；开两台 EC2，前面挂一个 ALB（负载均衡器），后面连一个 RDS 数据库。

### Step3: (50%):
- 最新的 SAA 题库

- SLA && Trade-off FinOps



－－－
－－－